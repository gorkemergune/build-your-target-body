# Copy this file to .env and fill in values
# IMPORTANT: When running on a physical device or emulator, replace localhost with your
# computer's local IP address (e.g., 192.168.1.100). Find it with: ipconfig (Windows) or
# ifconfig (macOS/Linux). Keep :8000 port.

EXPO_PUBLIC_API_URL=http://localhost:8000
